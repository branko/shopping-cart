import React, { Component } from 'react';
import Header from './Header';

class Shop extends Component {
  render() {
    return (
      <div id="app">
        <Header />
      </div>
    );
  }
}

export default Shop;
